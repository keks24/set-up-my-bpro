Place `_<some_name>` files for `/usr/local/share/zsh/site-functions/` here, any other files will be ignored.
Make sure that these files are owned by user `root` and group `staff` with access permissions `0644`
